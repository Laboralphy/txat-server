var User = require('../User.js');

test('basic', function(assert) {
	var u = new User();
	u.grantPowers(10);
	u.grantPowers(13);
	u.grantPowers(16);
	var a = u.getChannelList();
	assert.deepEqual(a, [10, 13, 16], 'added 3 channels');
});

